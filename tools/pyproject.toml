[project]
name = "artwork-matcher-tools"
version = "0.1.0"
description = "CLI tools for artwork matcher"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.0",
    "pillow>=11.0.0",
    "pandas>=2.2.0",
    "rich>=13.9.0",
]
