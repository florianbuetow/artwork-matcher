# Embeddings Service Configuration
# Environment variable overrides use prefix: EMBEDDINGS__
# Example: EMBEDDINGS__MODEL__DEVICE=cuda

service:
  name: "embeddings"
  version: "0.1.0"

model:
  name: "facebook/dinov2-base"
  device: "auto"                    # auto, cpu, cuda, mps
  embedding_dimension: 768
  cache_dir: "./data/models"          # Local model storage directory
  revision: "main"                  # Git revision (branch, tag, or commit hash)

preprocessing:
  image_size: 518                   # DINOv2 native input size
  normalize: true                   # Apply ImageNet normalization

server:
  host: "0.0.0.0"
  port: 8001

logging:
  level: "INFO"
  format: "json"
